<%= render partial: 'flash_message', locals: {messages: flash} %>
<h1>Blogs</h1>
<%= @blogs.count %> blogs   | <%= link_to new_blog_path do %>
	Create new blog
	<% end %>

<% @blogs.reverse_each do |blog| %>
	<%= render partial: "thumbnail", locals: {a: blog}%>
<% end %>	
